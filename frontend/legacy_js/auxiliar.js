// 🔹 Función para redirigir a una página del mismo proyecto
function irA(pagina) {
  window.location.href = pagina;
}

// 🔹 Función para regresar al inicio
function regresar() {
  window.history.back(); // Vuelve a la página anterior
}
